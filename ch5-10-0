
import requests 

r = requests.get('https://www.flag.com.tw')

if r.status_code == 200:    #回應狀態若為200表示OK
    print(r.text)   #將回應的網頁原始碼印出來
else:
    print(r.status_code, r.reason)    #若發生錯誤(狀態碼非200)，則印出狀態碼&錯誤原因。
    
    
    
